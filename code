<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StudySupport</title>
    <link rel="stylesheet" href="styles.css">
    <script src="https://cdn.jsdelivr.net/npm/swiper@8.4.5/swiper-bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@8.4.5/swiper-bundle.min.css" />
    <style>
        /* Основні стилі */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #1c1c1c;
            color: #fff;
            overflow-x: hidden;
            position: relative;
            background-image: url('your-background-image.jpg'); /* Додай реальний фон */
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
        }

        /* Контейнер для контенту */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Шапка сайту */
        header {
            text-align: center;
            padding: 50px;
            background-color: rgba(0, 0, 0, 0.6);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.7);
            position: absolute;
            width: 100%;
            top: 0;
            left: 0;
            z-index: 100;
        }

        header h1 {
            font-size: 3rem;
            text-transform: uppercase;
            color: #f47c48;
            text-shadow: 3px 3px 10px rgba(0, 0, 0, 0.8);
            animation: fadeIn 2s ease-in-out;
        }

        @keyframes fadeIn {
            0% { opacity: 0; }
            100% { opacity: 1; }
        }

        /* Слайдер */
        .swiper {
            width: 100%;
            height: 100%;
            margin-top: 150px;
        }

        .swiper-slide {
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 60px;
            animation: slideIn 2s ease-out;
        }

        @keyframes slideIn {
            0% { transform: translateX(100%); opacity: 0; }
            100% { transform: translateX(0); opacity: 1; }
        }

        .swiper-slide h2 {
            font-size: 3rem;
            margin-bottom: 20px;
            color: #f47c48;
            text-shadow: 2px 2px 15px rgba(0, 0, 0, 0.6);
            animation: textEffect 1.5s ease-out;
        }

        @keyframes textEffect {
            0% { transform: translateY(30px); opacity: 0; }
            100% { transform: translateY(0); opacity: 1; }
        }

        .swiper-slide p {
            font-size: 1.5rem;
            line-height: 1.6;
            max-width: 700px;
            margin: 0 auto;
            color: #ddd;
            animation: textEffect 1.5s ease-out;
        }

        /* Стиль для кнопок */
        .swiper-slide button {
            padding: 15px 30px;
            background-color: #f47c48;
            border: none;
            border-radius: 5px;
            color: #fff;
            font-size: 1.2rem;
            cursor: pointer;
            margin-top: 20px;
            transition: background-color 0.3s ease;
            transform: translateY(0);
            animation: buttonEffect 1.5s ease-out;
        }

        @keyframes buttonEffect {
            0% { transform: translateY(30px); opacity: 0; }
            100% { transform: translateY(0); opacity: 1; }
        }

        .swiper-slide button:hover {
            background-color: #f39c42;
        }

        /* Секція з відгуками */
        .reviews {
            background: rgba(0, 0, 0, 0.7);
            padding: 50px 0;
            text-align: center;
        }

        .reviews p {
            font-size: 1.3rem;
            color: #ddd;
            margin: 20px 0;
            line-height: 1.6;
            animation: fadeIn 2s ease-in-out;
        }

        /* Форма контакту */
        .contact-form {
            display: flex;
            flex-direction: column;
            padding: 20px;
            background-color: #333333;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }

        .contact-form input, .contact-form textarea {
            margin-bottom: 15px;
            padding: 10px;
            font-size: 1rem;
            border-radius: 5px;
            border: none;
            background-color: #444;
            color: #fff;
            transition: background-color 0.3s ease;
        }

        .contact-form input:focus, .contact-form textarea:focus {
            background-color: #555;
        }

        .contact-form button {
            padding: 15px;
            background-color: #f47c48;
            border: none;
            border-radius: 5px;
            color: white;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .contact-form button:hover {
            background-color: #f39c42;
        }

        /* Адаптивність */
        @media (max-width: 768px) {
            header h1 {
                font-size: 2.5rem;
            }

            .swiper-slide h2 {
                font-size: 2rem;
            }

            .swiper-slide p {
                font-size: 1.2rem;
            }

            .contact-form button {
                font-size: 1.2rem;
            }
        }
    </style>
</head>
<body>

    <header>
        <h1>StudySupport</h1>
    </header>

    <!-- Слайдер -->
    <div class="swiper">
        <div class="swiper-wrapper">
            <!-- Перший слайд -->
            <div class="swiper-slide">
                <div>
                    <h2>Ти не один — ми тут, щоб допомогти!</h2>
                    <p>Допоможемо виконати завдання будь-якої складності — швидко, якісно та з турботою про результат. Ми тут, щоб заощадити твій час і нерви.</p>
                    <button>Хочу оформити замовлення!</button>
                </div>
            </div>
            <!-- Другий слайд (відгуки) -->
            <div class="swiper-slide">
                <div>
                    <h2>Відгуки користувачів</h2>
                    <p>Коли я працював над магістерською з інженерії, виникало дуже багато складнощів з написанням, тому вирішив звернутись до спеціалістів. Зробили якісно і головне швидко. За два дні замовлення було в менше, моя магістерська пройшла антиплагіат на 97/100. Дякую, буду рекомендувати друзям!</p>
                    <p>Загайдачний Дмитро</p>
                </div>
            </div>
            <!-- Третій слайд (контактна форма) -->
            <div class="swiper-slide">
                <div>
                    <h2>Виникла проблема?</h2>
                    <p>Звертайтесь до нас! Наші менеджери допоможуть з будь-якими питаннями щодо нашої платформи чи послуг.</p>
                    <form class="contact-form">
                        <input type="email" placeholder="Ваша пошта" required>
                        <input type="text" placeholder="Ваше ім'я" required>
                        <textarea placeholder="Ваше запитання" required></textarea>
                        <button type="submit">Надіслати</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script>
        const swiper = new Swiper('.swiper', {
            loop: true,
            autoplay: {
                delay: 5000,
            },
            pagination: {
                el: '.swiper-pagination',
                clickable: true,
            },
        });
    </script>

</body>
</html>
